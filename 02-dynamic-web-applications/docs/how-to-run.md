# How to run

```bash
make build # Build PHP environment.
make up # Build and run PHP environment. Application is hosted on localhost:80.
make stop # Stop PHP environment.
make down # Delete PHP environment (containers, networks, volumes).
```

